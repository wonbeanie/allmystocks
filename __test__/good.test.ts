describe('js test', () => {
    it('이건 테스트 타이틀 메시지1', () => {
        expect(3 + 4).toBe(7);
    });

    it('이건 테스트 타이틀 메시지2', () => {
        const name = 'yuds';
        expect(name).toBe('yuds');
    });
});